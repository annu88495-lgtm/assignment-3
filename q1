using System;

public static class StudentUtility
{
    public static double CalculatePercentage(double obtainedMarks, double totalMarks)
    {
        return (obtainedMarks 
    }

    public static string CalculateGrade(double percentage)
    {
        if (percentage >= 90) return "A";
        if (percentage >= 80) return "B";
        if (percentage >= 70) return "C";
        if (percentage >= 60) return "D";
        return "F";
    }

    public static double CalculateAttendancePercentage(int presentDays, int totalDays)
    {
        return ((double)presentDays / totalDays) * 100;
    }
}

 
double percentage = StudentUtility.CalculatePercentage(450, 500);
Console.WriteLine($"Percentage: {percentage}%");
Console.WriteLine($"Grade: {StudentUtility.CalculateGrade(percentage)}");
Console.WriteLine($"Attendance: {StudentUtility.CalculateAttendancePercentage(80, 90)}%");
